#include <bits/stdc++.h>

using namespace std;

int main ()
{
    size_t n, m;
    cin >> n >> m;
    vector<pair<size_t, size_t>> observatories(n + 1);
    for (size_t i = 1; i <= n; ++i)
        cin >> observatories[i].first >> observatories[i].second;
    
    
}